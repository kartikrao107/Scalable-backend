services:
  backend:
    build:
      context: .
      args:
        DATABASE_URL: "postgresql://postgres:ANDROIDk%401@postgres:5432/ecommerce"

    container_name: ecommerce_backend

    ports:
      - "3000:3000"

    depends_on:
      - postgres
      - redis

    environment:
      DATABASE_URL: "postgresql://postgres:ANDROIDk%401@postgres:5432/ecommerce"
      REDIS_URL: "redis://redis:6379"
      JWT_SECRET: "your_secret"
      CLOUDINARY_CLOUD_NAME: "your_cloud_name"
      CLOUDINARY_API_KEY: "your_api_key"
      CLOUDINARY_API_SECRET: "your_api_secret"

  postgres:
    image: postgres:16
    container_name: postgres_db
    restart: always

    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: "ANDROIDk@1"
      POSTGRES_DB: ecommerce

    ports:
      - "5432:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:latest
    container_name: redis_server

    ports:
      - "6379:6379"

volumes:
  postgres_data:
# version: "3.8"

# services:
#   postgres:
#     image: postgres:16
#     container_name: postgres-db
#     restart: unless-stopped
#     environment:
#       POSTGRES_USER: postgres
#       POSTGRES_PASSWORD: ANDROIDk@1
#       POSTGRES_DB: ecommerce
#     ports:
#       - "5432:5432"
#     volumes:
#       - postgres_data:/var/lib/postgresql/data

# volumes:
#   postgres_data: